import java.util.*;
public class DoublyLinkedListDriver {
    public static void main(String[] args){
        String s=null;
        DoublyLinkedList list=new DoublyLinkedList();
        DoublyLinkedList list1=new DoublyLinkedList();
            list.addFront(5);
            list.addFront(3);
            list.addFront(2);
            list.addFront(8);
            list.addBack(1);
            System.out.println(Collections.binarySearch(list, 3);
            System.out.println(list);
            System.out.println(list.checkIfNull(list.getHeader()));
        System.out.println(list.checkIfNull(list.getTrailer()));
        System.out.println(list.checkIfNull(list.getHeadNextNext()));
        System.out.println(list.checkIfNull(list.getNext()));
        System.out.println(list.checkIfNull(list.getTrailPrev()));
        list.Merge(list.getHeader());
        System.out.println(list);
        }}
